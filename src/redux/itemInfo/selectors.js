export const isLoading = (state) => state.catalog.isLoading;
export const error = (state) => state.catalog.error;
export const catalogAllData = (state) => state.catalog.catalogAllData;
export const catalogDataById = (state) => state.catalog.catalogDataById;
